# This file is machine-specific and must be replaced before deployment.
#
# On the target machine, generate it with:
#   nixos-generate-config --show-hardware-config > nixos/hardware-configuration.nix
#
# Or after a standard NixOS install, it will be at /etc/nixos/hardware-configuration.nix
{ ... }: { }
